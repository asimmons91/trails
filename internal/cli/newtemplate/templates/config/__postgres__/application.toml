[views]
layout = "application"

[server]
host = "127.0.0.1"
port = 3000

[database]
host = "localhost"
port = 5432
user = "postgres"
password = "postgres"
name = "app"
sslmode = "disable"

[mailer]
delivery = "log"
from = "from@example.com"

[cache]
store = "memory"
ttl = "5m"
