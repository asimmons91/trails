#!/usr/bin/env bash
#MISE description="Validate the GoReleaser config and confirm the CLI still builds for the current platform"

set -e
goreleaser check
goreleaser build --single-target --snapshot --clean
